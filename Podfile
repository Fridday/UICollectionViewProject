# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UICollectionViewProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UICollectionViewProject

  target 'UICollectionViewProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UICollectionViewProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'SDWebImage'
  pod 'Reachability', '~> 3.2'
  
end
